.reindex(list) - will leave leave only the rows that are in the new index.
https://pandas.pydata.org/pandas-docs/stable/generated/pandas.DataFrame.reindex.html

# For working with dict
dict.items() <- creates a list of items
for idx, vals in k_rmse_result.items(): <- extracts two values from dictionary - idx and values
vals.keys <- extracts keys from dictionary values


